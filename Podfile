platform :ios, '8.0'
  
def share_pods
  pod 'SVProgressHUD', :inhibit_warnings => true
  pod 'OneSignal'
  pod 'Masonry', :inhibit_warnings => true
end

target "OneMoreFramework" do
  share_pods
end
